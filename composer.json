{
    "name": "quantaquirk/testing",
    "description": "The QuantaQuirk Testing package.",
        "license": "proprietary",
    "authors": [
        {
            "name": "Tomáš Selič (Smisch-DEV)",
            "email": "contact@smisch.sk"
        }
    ],
    "require": {
        "php": "^8.1",
        "ext-mbstring": "*",
        "quantaquirk/collections": "^10.0",
        "quantaquirk/contracts": "^10.0",
        "quantaquirk/macroable": "^10.0",
        "quantaquirk/support": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "QuantaQuirk\\Testing\\": ""
        }
    },
    "extra": {
        "branch-alias": {
            "dev-master": "10.x-dev"
        }
    },
    "suggest": {
        "brianium/paratest": "Required to run tests in parallel (^6.0).",
        "quantaquirk/console": "Required to assert console commands (^10.0).",
        "quantaquirk/database": "Required to assert databases (^10.0).",
        "quantaquirk/http": "Required to assert responses (^10.0).",
        "mockery/mockery": "Required to use mocking (^1.5.1).",
        "phpunit/phpunit": "Required to use assertions and run tests (^9.5.8|^10.0.7)."
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "dev"
}
